# GLM Quota Monitor — 项目约束文件

## 开发流程约束

### C1: 数据验证先行
**任何涉及 API 数据的功能，必须先用独立测试验证数据正确，再写 UI 代码。**
- 新功能开发顺序：API 测试 → 解析逻辑 → 单元验证 → UI 集成
- 禁止"先写 UI 再看数据"的流程
- 每次修改解析逻辑后，必须运行测试确认输出

### C2: 三道门禁（代码项目必须遵守）
**所有代码变更必须经过三道门禁，每道门禁都是子代理全部 PASS 才算通过。**

#### 门禁 1：计划文档
- 写出完整的计划文档（改什么、为什么改、怎么改）
- 子代理审查计划的完整性、正确性、空间计算等
- 全部 PASS 后才能进入门禁 2

#### 门禁 2：实现计划
- 写出具体的实现计划（改哪些行、常量值、代码片段）
- 子代理审查行号引用、逻辑正确性
- 全部 PASS 后才能进入门禁 3

#### 门禁 3：代码审查
- 写完代码后，派多个子代理从不同角度审查（布局、GDI、线程安全等）
- 全部 PASS 后才能提交推送
- 审查不过就改，改完重新审查，循环直到全部 PASS

**流程顺序：计划 → 门禁1 → 实现计划 → 门禁2 → 写代码 → 门禁3 → 编译 → 提交推送**

禁止：
- 跳过任何门禁
- 先提交再审查
- 带着已知问题提交

### C3: 非代码项目
- 非代码项目（文档、配置等）不需要三道门禁
- 不确定是否需要门禁时，必须先问用户确认

### C4: 视觉设计确认
**UI 变更必须先出设计稿（HTML demo），确认后再实现。**
- 不允许直接写代码然后让用户"看看效果"
- 设计稿需要覆盖正常状态和异常状态（离线、超限等）

## 技术约束

### T1: GDI 资源管理
- 所有 Brush、Font、Pen、GraphicsPath 必须 using 声明
- 禁止内联 `new SolidBrush(...)` 作为方法参数
- 缓存的字体必须在 Dispose 中释放

### T2: 线程安全
- QuotaService 的回调在后台线程触发
- UI 更新必须通过 SynchronizationContext.Post 到 UI 线程
- SystemEvents.UserPreferenceChanged 可能在后台线程触发

### T3: 配置一致性
- SettingsForm 保存时必须保留 FloatingBarX/Y
- ConfigService.Save 失败时不更新内存、不触发事件
- 配置变更必须触发完整的服务重启（轮询+UI）

### T4: 密钥安全
- config.json 在 .gitignore 中，不能提交
- 代码中不能硬编码任何 API Key、Token、密码
- 读取密钥优先级：配置文件 > 环境变量

## API 数据约束

### A1: 字段兼容
API 字段名可能变化，必须支持多种别名：
- `usage` / `limit_value` / `limitValue`
- `currentValue` / `used_value` / `usedValue`
- `remaining` / `remaining_value` / `remainingValue`
- `percentage`（直接百分比，优先使用）

### A2: 多 limit 类型
quota/limit 可能返回多个同类型 limit：
- TOKENS_LIMIT unit=3 → 5h 流控（取第一个）
- TOKENS_LIMIT unit=6 → 日流控（忽略）
- TIME_LIMIT → MCP 月度配额

### A3: 数值类型兼容
JSON 数值可能是 int、double 或 string，GetLongAny 必须全部处理。

## 发布约束

### R1: 版本号
- 使用语义化版本：v0.1.0（初始版本）
- 中文 Release 标题和说明

### R2: 两版发布
- 轻量版：框架依赖单文件（需 .NET 8 运行时）
- 完整版：自包含单文件（零依赖）
- GitHub Actions 自动构建发布
