# 实现计划：浮动 Widget 布局修复

## 修改内容

仅修改 FloatingWidget.cs 两个位置：

### 修改 1：第 161 行
```
当前：y += 16;
改为：y += 22;
```
位置：DrawQuotaSection 方法内，标签行之后、进度条+百分比行之前

### 修改 2：第 187 行
```
当前：y += 14;
改为：y += 18;
```
位置：DrawQuotaSection 方法内，进度条+百分比行之后、详情行之前

### 第 203 行 `return y + 14`：不修改

理由：每区总高 = 22 + 18 + 14 = 54px。两个区 + 间距 + 统计 = 54 + 10 + 10 + 54 + 10 + 10 + 14 + 28(padding) = 190px，刚好填满 CardHeight=190。改大会超出窗口。

## 不修改

- 常量（BarWidth=120, BarHeight=6, BarPctGap=10, CardHeight=190）已是正确值
- 字体、颜色、配色不变
- 其他文件不变
